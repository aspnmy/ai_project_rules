# WSL 配置文件目录

本目录用于存放与 Windows Subsystem for Linux (WSL) 相关的配置文件。

## 包含的文件

- **wsl_config.json**: WSL 的主要配置文件
- **wsl-distro.info**: WSL 发行版信息
- **wsl_dev_manager.sh**: WSL 开发环境管理脚本

## 使用说明

将所有与 WSL 相关的配置文件存放在此目录中，以便统一管理和维护。