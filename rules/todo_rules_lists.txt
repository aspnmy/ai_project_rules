todo：PG_sub_rulesfiles 参考其中登记次级规则路径登记规则，从PG_RuleFileName中分离功能性规范到rules\目录下新增对应的次级规则文件，并且把次级文件路径登记到PG_sub_rulesfiles中，最后测试是否能够按照次级规则从查询原则 提高本框架辅助指导开发代码中的效率

todo：新增效率规则：PG_ProjectMod中的值，如果是devP,PG_RuleFileName，那么就加载PG_RuleFileName为唯一规范文件；如果是proD,PG_ProdProjectRuleFileName，那么就加载PG_ProdProjectRuleFileName为唯一规范文件；

todo：新增自我进化规则及工作脚本： Self-evolution.py 此脚本的功能是监控PG_sub_rulesfiles中的注册的规则文件，如果有更新修改，就自动对本项目中的文件进行自我检查更新。第二是监控本项目和被开发的项目中的文件修改状态，如果有修改的情况就自动调用此脚本文件根据PG_sub_rulesfiles文件进行自我检查和更新，本脚本需要判断PG_ProjectMod,因为两种模式下的自我进化，除了PG_sub_rulesfiles是一致的，但是规范解释文件是不一致的，所以新增的规则加入的规范解释文件是可能会不一致的，如果不正确添加可能会造成异常错误。完成此脚本后进行调试验证，验证成功后把此脚本编译成独立exe文件在后台常驻执行监控，当此脚本监控到需要自我进化的文件时，把需要自我进化的文件路径，写入到 rules\todo_Self-evolution_lists.txt中,然后传输指令”执行rules\todo_Self-evolution_lists.txt中的todo“，知道AI自己执行。此指令的优先级在devP模式下应该高于人工，todo_Self-evolution_lists.txt自我进化的路径和人工正在对话或编辑的路径冲突时，先执行自我进化脚本；此指令在proD模式中权限低于人工，只允许在人工不对话，不操作的情况下执行。实现此功能后将此规则加入规范文件中并生成次级规则文件，注册到PG_sub_rulesfiles中

todo:对项目根目录下面递归的所有文件结构 路径结构进行合规性检查 如果有新增的文件类型没有对应的合规规则，则需要更新规则在规范文件中，并合并相同功能的规则到次级规则文件中，并注册次级规则文件路径