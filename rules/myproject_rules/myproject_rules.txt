# 开发项目私有规范配置文件 - 重新初始化
# 格式结构：${配置项}|${配置值}

# 项目路径变量 - 默认路径
My_ProjectPath|project_var|"..\builder\devWinWsl2"

# 规范类型配置 (Strict, myproject_rules, all) - 默认使用all类型
My_SpecType|project_var|"all"

# 项目类型配置 - 支持以下值：
# nil: 不使用特定项目类型配置，按最小化配置处理
# Default: 使用默认项目类型配置
# 其他特定项目类型: 使用对应的项目特定配置
# 当配置是nil、Default或My_ProjectType不存在时，按最小化配置处理
My_ProjectType|project_var|"nil"

# 私有规则定义 - 默认路径
My_RuleFileName|project_var|"..\builder\devWinWsl2\myproject_rules.md"

# 是否执行全局规范的绝对性 - 默认不强制执行
My_UseGlobalRules|project_var|"false"

# 代码规范遵循配置 (true/false) - 默认遵循全局代码规范
My_UseGlobalCodeSpec|project_var|"true"

# 路径结构遵循全局规范 (true/false) - 默认遵循全局路径规范
My_UseGlobalPathSpec|project_var|"true"

# 文档规则遵循全局规范 (true/false) - 默认遵循全局文档规范
My_UseGlobalDocSpec|project_var|"true"

# README规则遵循全局规范 (true/false) - 默认遵循全局README规范
My_UseGlobalReadmeSpec|project_var|"true"

# 文件隔离规范 (true/false) - 默认启用文件隔离
# 启用后，系统将以My_RuleFileName中记录的文件的同级目录为根目录，仅向下递归查找内容，不向上查找
My_FileIsolationEnabled|project_var|"true"

# 初始化时间戳
My_InitTime|project_var|"重新初始化完成 - 添加文件隔离规范"